/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['media.licdn.com', 'imgur.com', 'i.imgur.com', 'images.pexels.com'],
    },
  };
  
  export default nextConfig;
  